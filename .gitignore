# Bağımlılıklar
node_modules/

# Astro / build çıktıları
dist/
.astro/

# Ortam değişkenleri (gerçek değerler asla commit'lenmez)
.env
.env.local
.env.*.local

# Test / kapsama
coverage/

# Editör / işletim sistemi
.DS_Store
.vscode/*
!.vscode/extensions.json
*.log
npm-debug.log*

# Claude Code — makineye özel yerel ayarlar (paylaşılan .claude/ kalabilir)
.claude/settings.local.json

# Cloudflare Pages
.wrangler/

# Kişisel veri — bu depo HERKESE AÇIK (GitHub Pages deposu).
# CV; fotoğraf, telefon, LinkedIn ve özlük bilgileri içeriyor. Yerelde
# durabilir (Hakkında metni buradan yazıldı) ama commit'lenmemeli:
# git geçmişine giren dosyayı sonradan temizlemek zordur.
ONURCANYILMAZ-CV.pdf
